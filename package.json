{
    "name": "unix-js-base-filesystem",
    "module": "src/index.ts",
    "type": "module",
    "version": "0.0.1",
    "description": "This is a base filesystem for unix.js. It performs basic I/O and contains some UNIX coreutils.",
    "author": "p-rivero",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/p-rivero/unix-js-filesystem.git"
    },
    "dependencies": {
        "unix-core": "^0.0.7"
    },
    "devDependencies": {
        "@types/node": "^20.11.24",
        "unix-parser": "^0.0.5"
    },
    "scripts": {
        "bundle": "unix-parser ROOT -o dist/filesystem.cjs --indent 0"
    }
}
