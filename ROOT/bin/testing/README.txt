This directory contains sample files for testing that your setup is working correctly. 

By default it's not included in the final build, but you can include it by defining the `UNIXJS_TESTING` environment variable before building.
